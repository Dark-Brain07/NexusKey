{
  "name": "NexusKey",
  "private": true,
  "version": "0.1.0",
  "description": "NexusKey — a bonded verification registry for rental authority, backed by a GenLayer Intelligent Contract.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "pnpm --filter @nexuskey/shared build",
    "dev:web": "pnpm build:shared && pnpm --filter @nexuskey/web dev",
    "dev:api": "pnpm build:shared && pnpm --filter @nexuskey/api dev",
    "build": "pnpm build:shared && pnpm --filter @nexuskey/api build && pnpm --filter @nexuskey/web build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "format-check": "prettier --check .",
    "format": "prettier --write .",
    "test": "pnpm -r test",
    "test:contract": "pnpm --filter @nexuskey/api exec true && cd contracts/NexusKey && python3 -m pytest tests -v"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
